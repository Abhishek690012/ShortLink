# System-level dependencies for building, running and load testing ShortLink.
# These represent the binaries and services required to run this project.
#These should not be installed via pip. These should be present as system dependencies.
k6>=0.47.0
postgresql>=15
redis>=7
docker-compose>=2.0
